apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: detection-experiments-pvc
  namespace: detection2
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 7Ti
  volumeName: detection-experiments-pv
  storageClassName: ""
